# Unique Word Collector Pro

A desktop application built with Python and Tkinter that extracts and counts unique words from text input or uploaded files. 
This project demonstrates **Object-Oriented Programming (OOP)**, **Unit Testing**, and **Regex-based text processing**.

## 🚀 Features
- **File Support**: Upload `.txt` files to analyze large volumes of text instantly.
- **Data Integrity**: Uses Regular Expressions to strip punctuation and handle case sensitivity.
- **Performance**: Optimized using Python `sets` for $O(1)$ average time complexity for word lookups.
- **Unit Tested**: Includes a robust test suite to ensure logic accuracy.

## 🛠️ Tech Stack
- **Language:** Python 3.x
- **GUI:** Tkinter
- **Testing:** Pytest
- **Pattern Matching:** Regular Expressions (re)

## 📦 Installation & Setup
1. Clone the repository:
2. Install dependencies 
 bash```pip install -r requirements.txt```
3. Run tests (Optional) 
 bash```python -m pytest```


